#!/usr/bin/env python3

#
# Copyright (C) 2025 Lee M. Lwando
# SPDX-License-Identifier: GPL-3.0-or-later
#

import json
import sys
from datetime import datetime

# Read input from stdin
input_data = json.load(sys.stdin)

webhook_id = input_data["id"]

# TODO: Replace with actual webhook triggering logic
# For now, simulate triggering a webhook
print(f"DEBUG: Would trigger webhook with ID: {webhook_id}", file=sys.stderr)

# Return success with timestamp
result = {
    "success": True,
    "webhook_id": webhook_id,
    "triggered_at": datetime.utcnow().isoformat() + "Z",
    "response_status": 200,
    "response_message": "Webhook triggered successfully (simulated)"
}

json.dump(result, fp=sys.stdout)
